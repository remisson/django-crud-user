
# Django user CRUD

Django app generic user CRUD based on Model-View-Controller (MVC) architectural pattern with unit tests and using bootstrap version 4.0. Basic fluxogram with authentication, account creation, username edit and account exclusion.

` Python 3.* `

` Django 6.1.* `

` Bootstrap 4.0 `
